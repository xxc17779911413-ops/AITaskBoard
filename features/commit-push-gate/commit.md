# 提交记录（commit-push-gate）

- feat(commit-push-gate): 代码推送门禁——判定已登记提交是否真的到了远程（`pushed`/`not_pushed`/`unknown` 三态，未登记仓库/无远程/sha 本地不存在归入 unknown 且阻塞但不冒充通过）；`server/git.mjs` 新增 `commitPushState`（先 `rev-parse` 兜 `sha-not-found`，再查 `refs/remotes/*`；只读不 fetch/不 push），`server/ops.mjs` 新增 `getNodePushGate`（按 repo+sha 去重回填来源节点、`scope=self|subtree`、空态 `ready=null`、纯读不 bump revision）+ `renderPushGateMd`（表格单元格转义）；三入口 1:1（HTTP `/api/nodes/:id/push-gate`、CLI `push gate`、MCP `commit_push_gate`）+ 随附 prd/design 文档与决策 36；补 15 条聚合/UT + 4 条三入口集成测试
- fix(commit-push-gate): 收口独立测试 D1/D2——`reason` 从笼统的 `repo-path-missing` 细分为稳定且可行动的枚举：`repo-path-unset`（已登记但没填 `localPath`，先 `repo update`）/ `repo-path-missing`（填了但路径无效）/ `git-unavailable`（本机无 git）/ `repo-not-registered` / `no-remote` / `sha-not-found` / `git-error`，映射由新增的 `pushReasonFromError(e, repo)` 单点完成（`REPO_PATH_MISSING` 结合 `repo.localPath` 才能分清「没填」与「填了但无效」）；`detail` 优先用真实 `error.message`，不再是一句笼统分类文案；补 4 条 D1/D2 回归 UT（在旧实现上 3/3 失败）
- feat(commit-push-gate): 收口决策 36 预留的下一步——把推送判定并入 `delivery_gate` 成为第四个来源（决策 38），交付结论不再漏掉「登记提交没 push」；`ops.buildDeliveryGateFull` 复用 `getNodePushGate` 产出证据注入同步聚合，`not_pushed`/`unknown` 一律阻塞
- docs(commit-push-gate): 修正 `design.md` R6 的过期陈述——原文仍写「首版不并进 delivery_gate、totals.sources 恒为 3、既有断言 notApplicable: 3、并入留给下一步」，与决策 38 已收口的事实矛盾；改为「首版刻意推迟的理由（历史）」+「已由决策 38 收口：push 为第四个来源、sources 3→4、not_pushed/unknown 阻塞」并回指 `../delivery-gate/`；关联章节的决策引用补上 38。纯文档改动，无代码/契约变化
